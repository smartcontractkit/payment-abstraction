ccipReceive.t.sol
├── when the sender is not allowlisted
│   └── it should revert
└── when the sender is allowlisted
    ├── when receiving a single asset
    │   ├── it should emit an AssetReceived event
    │   └── it should emit a MessageReceived event
    └── when receiving multiple assets
        ├── it should emit an AssetReceived event for each asset
        └── it should emit a MessageReceived event
